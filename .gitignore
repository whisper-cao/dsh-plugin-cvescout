# 依赖与构建产物
node_modules/
*.tsbuildinfo
*.tgz

# 测试与运行期产物
.smoke-cache.json
*.log
# 测试用自签证书（运行时生成，绝不入库）
test/fixtures/.certs/

# 本地配置（可能含 NVD Key / GitHub Token / npm token）
.env
.env.local
cordis.local.yml
.npmrc
.npmrc.local

# 系统噪声
.DS_Store
Thumbs.db
